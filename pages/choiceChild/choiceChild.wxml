<view style="height:5rem"></view>

<van-grid gutter="{{ 10 }}" column-num="2">
  <van-grid-item icon="user-o" text="{{ item.name +  '   ' + '设备'+  ':' + item.deviceId}}" wx:for="{{ childList }}" wx:for-index="index" data-deviceId="{{item.deviceId}}" bind:tap="navigatorPage"/>
</van-grid>